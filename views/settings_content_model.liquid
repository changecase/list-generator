import QtQuick 2.0

ListModel {
  ListElement{
    label: "{{content_label}}"
    parentLabel: "{{parent_name}}"
    parentLevel: {{parent_level}}
    interactionComponent: "{{control_type}}"
    values: [
      {% for value in values %}
        ListElement{value: "{{value}}"}{% if values.size > 1 %},{% endif %} 
      {% endfor %}
    ]
    state: 0
  }
}
